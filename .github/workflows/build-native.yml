name: Build IRS Spect Native

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Reassemble native source
        run: |
          cat ci/src.b64.00 ci/src.b64.01 ci/src.b64.02 ci/src.b64.03 ci/src.b64.04 > /tmp/irs-native.b64
          base64 -d /tmp/irs-native.b64 > /tmp/irs-native.tar.gz
          gzip -t /tmp/irs-native.tar.gz
          mkdir -p native
          tar -xzf /tmp/irs-native.tar.gz -C native
          cp ci/NativeApi-1.5.1.java native/app/src/main/java/ir/dinavo/fraudwatch/NativeApi.java
          sed -i '/android:debuggable="false"/d' native/app/src/main/AndroidManifest.xml
          sed -i 's/[[:space:]]*package="ir.dinavo.fraudwatch"//' native/app/src/main/AndroidManifest.xml
          sed -i 's/versionCode = 14/versionCode = 15/' native/app/build.gradle.kts
          sed -i 's/versionName = "1.5.0"/versionName = "1.5.1"/' native/app/build.gradle.kts
          sed -i 's/APP_VERSION_CODE=14/APP_VERSION_CODE=15/' native/app/src/main/java/ir/dinavo/fraudwatch/MainActivity.java
          sed -i 's/APP_VERSION_NAME="1.5.0"/APP_VERSION_NAME="1.5.1"/' native/app/src/main/java/ir/dinavo/fraudwatch/MainActivity.java
          printf '%s\n' '{"package_id":"ir.dinavo.fraudwatch","version_code":15,"version_name":"1.5.1"}' > native/app/src/main/assets/irs_spect_release.json

      - name: Build unsigned release APK
        working-directory: native
        run: gradle --no-daemon :app:assembleRelease

      - name: Verify APK output
        run: |
          APK="native/app/build/outputs/apk/release/app-release-unsigned.apk"
          test -f "$APK"
          echo "APK_SIZE=$(stat -c%s "$APK")"
          sha256sum "$APK"

      - name: Upload unsigned APK
        uses: actions/upload-artifact@v4
        with:
          name: IRS-Spect-Native-1.5.1-unsigned
          path: native/app/build/outputs/apk/release/app-release-unsigned.apk
          if-no-files-found: error
          retention-days: 7
